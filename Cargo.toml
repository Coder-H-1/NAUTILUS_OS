[package]
name = "rpi_os"
version = "0.1.0"
edition = "2021"

[dependencies]
font8x8 = { version = "0.3", default-features = false, features = ["unicode"] }

[profile.release]
panic = "abort"
lto = true
